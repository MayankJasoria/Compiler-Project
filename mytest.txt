
<<<driver program>>>
start
	declare	low, high, x, k, m, n: integer;
	get_value(low);
	get_value(high);
	declare A, B: array[low..high] of integer;
	get_value(A);
	for(k in 1..3)
	start
		get_value(m);
		get_value(n);
		x:= A[m]+B[n];
		print(x);                           **As long as there is no run time error with respect to the bounds, x, y, z get printed**
	end
end